# Backlog

Proposed changes that serve none of the North Star tracks (A/B/C in
`AGENTS.md`) are logged here instead of being implemented — per the operating
contract and reviewer Gate 4. One row per idea; delete the row when it ships
or is rejected for good.

| logged | idea | why it's parked (which track it fails) |
|--------|------|----------------------------------------|
| 2026-07-07 | A real `cw update` self-update verb (npm-based, as the old build's captured help page said: "Update cool-workflow to the latest release via npm", `plugins/cool-workflow/project/docs/rebuild/SPEC/cli-help/update.txt`) | Serves no track (A/B/C) — it is install care, not pipeline, recovery, or manifest work. The v2 rebuild shipped with `update` in `cw help` and KNOWN_COMMANDS but with NO code behind it (`cw update` said "Unknown command: update. Did you mean: update?" — a hint that points at itself). The dead lines were taken out (help.ts, parseargv.ts) and a regression test in `cli-arg-parsing-smoke.js` now keeps them out. If this ever ships for real it needs: a dispatch arm or capability-table row, a `docs/*.7.md` section (man pages are the contract), a smoke, and the `_root.txt` conformance fixture put back with the new lines. |
| 2026-06-14 | Consolidate cross-module util look-alikes (`countBy`×7, `compact`×5+, `truncate`×4, `mergeById`×3, `isRecord`×4, `clone`×2) into a shared `util.ts` single source | Parked, not rejected — but DANGEROUS to do blindly and low net value, so it serves no North Star track today. Investigation (2026-06-14 refactor sweep) found the look-alikes are NOT uniformly identical: `coordinator.ts` `compact` also drops empty arrays while the others keep them; `multi-agent.ts` `compact` returns `undefined` for falsy input; the four `truncate`s use different limits (64/80/200/max) and only some normalize whitespace. A naive "dedup all" would silently change behavior — the exact false-green the project forbids. Only `clone` + the byte-identical `countBy`/`mergeById` bodies are safe, and those are trivial 2–3 line pure utilities where local duplication is cheap and a shared module only adds coupling (small-kernel ethos). Revisit only with a per-function byte-identity proof and a behavior-preserving gate; never merge by name. |
| 2026-06-22 | `PipelineContract.commitMessageTemplate` — implement OR delete the unread field | Parked, rejected-for-now. Declared v0.1.68 (auto-generated commit messages with `{{runId}}/{{completedTasks}}/{{totalTasks}}`) but never wired to a consumer. #258 implemented a `renderCommitMessage()` + `PipelineStageRunResult.commitMessage`, but NO caller reads it (CLI/MCP/report/orchestrator all ignore it) — an orphaned surface, so #258 was reverted. A pure type-only DELETE is currently blocked by the onramp `types-without-runtime` gate (a type-only src change is "not a valid cycle"). Do NOT implement without a real consumer (that would re-add a parsimony-violating dead surface). When cleaning: either pair the delete with genuine runtime work, or first add a gate escape-hatch that allows type-only deletion of BACKLOG-acknowledged dead fields. |
| 2026-07-07 | Adopt a code formatter (e.g. Prettier) across `src/` | Parked, rejected-for-now. Serves no North Star track — it is style, not pipeline/recovery/manifest work. AGENTS.md's own rule against gratuitous reformatting means a first run would touch most of the ~130 `src/**/*.ts` files at once, destroying `git blame` over 43k+ lines for zero behavior value, and every other gate in this repo is a bespoke zero-dependency node script (matching "tools, not frameworks") — a formatter would be the first framework dependency added for pure style. Revisit only if a real recurring style-drift problem shows up (none observed as of the architecture-improvement plan's structure-track work, 2026-07-07). |
| 2026-07-07 | Burn down the remaining `core/**` clock/env baseline entries in `scripts/purity-baseline.json` (`core/state/state-node.ts` 5× `new Date()`, `core/trust/evidence-grounding.ts` 3× `process.env`, plus one each in `candidate-scoring.ts`/`pipeline/runner.ts`/`migrations.ts`/3 `state-explosion/*.ts` files) | Parked, not rejected. `scripts/purity-gate.js` tracks these as a committed, itemized baseline rather than pretending they don't exist — most are `now \|\| new Date().toISOString()`-shaped fallbacks (an injectable default parameter pattern the rebuild docs call out as an accepted exception, not a bug) or `env: NodeJS.ProcessEnv = process.env` default-parameter positions. Burning them down means threading an explicit `now`/`env` parameter through every caller of each function — real, if mechanical, work, out of scope for the gate's own PR. Revisit file by file, shrinking the baseline (and re-running `purity:check`, which fails closed on a baseline entry that no longer matches reality) as each is fixed. |
| 2026-07-13 | An async acquire path for `withFileLock` (`withFileLockAsync`, `src/shell/fs-atomic.ts`) so a long-lived process — the MCP server, `src/mcp/server.ts` — does not freeze its event loop during a contended lock wait | Parked, not rejected — serves no North Star track directly (internal robustness, not pipeline/recovery/manifest) and is a large, higher-risk refactor. The 2026-07-13 perf-audit backoff fix cut the TYPICAL freeze and the frequency of long ones, but the acquire is still synchronous (`Atomics.wait`), so under PERSISTENT contention the calling thread — and the MCP server's whole event loop, including SIGINT/SIGTERM dispatch (`drive.ts:876-893`) — can still block for up to ~6s. The real fix is an async acquire that `await`s a real `setTimeout` instead of `Atomics.wait`, sharing `withFileLock`'s identical acquire/steal/verify body. But it only pays off once the MCP hot paths (`saveCheckpoint`, `withRunStateLock`, trust-audit append, telemetry — all synchronous today, called deep inside the ~197 synchronous tool handlers) go async up their whole call chains: ~20 call sites, and a half-converted chain is exactly where a silent state-corruption or a deadlock would hide — the failure mode the audit flagged. Needs its own `SPEC/state-core.md` pass (the lock protocol is byte-compat pinned), a design, and phased PRs; do NOT bundle it into a mechanical perf fix. Re-entrancy between a sync `withFileLock` and an async `withFileLockAsync` on the SAME target also needs a defined rule (`HELD_LOCKS` is one per-process set today). In fleet operation, where many parallel sessions run together with shared workers, the sync event-loop becomes a product ceiling — the whole control plane's speed is limited by lock contention from one session. This makes the async path a playbook requirement, not just a robustness fix. |
| 2026-09-01 | Bring `schedule`, `routine`, and `sched` into one shape: "CW evaluates, the platform triggers" with CI cron and webhooks as the shared trigger layer | Parked, not rejected — serves no North Star track before a unified design is laid out. The three verbs handle different work today: `schedule` runs loops, cron work, and one-shot reminders (stored in `.cw/schedules/tasks.json`); `routine` runs trigger-based work from API and GitHub webhooks (stored in `.cw/routines/triggers.json`); `sched` runs policy for the run queue (stored in `registry/scheduling-policy.json`). They have shared needs — when to run, what to do if it fails, when to try again — but use separate code and storage. A unified design — "CW evaluates the next action, the platform triggers it" — would let CI cron and webhooks be the one shared layer that fires the work. Before any work starts, needs a North Star track argument (pipeline/recovery/manifest work) and a story of who would benefit. |
| 2026-09-01 | Real containment for workflow app code (`workflow.js`), not just the honesty label this row's own change adds | Parked, not rejected — serves the recovery track in the long run, but is a big, high-risk build, out of scope now. Today `workflow-app-loader.ts` runs app code in the same process, with full power over the host; the run record now says so in plain words (`appCode.execution` = `"in-process-unsandboxed"`), but nothing stops the code itself. Two real paths forward: (1) a child-process app host that runs `workflow.js` in its own process, using the sandbox env plumbing already built for delegated agent workers (`worker-isolation.ts`) to bound what it may read, write, and run; or (2) a declarative app manifest, where an app's phases and tasks stay plain data and CW never runs app-owned code at all — at the cost of the current factory-function app shape. Revisit when a real workflow app is shown to need less than full host power, or when a security review names in-process app code as the top open risk. |
| 2026-09-02 | A wrapping mechanism for the `-q` headline help line before a SIXTH vendor flag is added | Parked, not rejected — no sixth vendor is asked for yet, so there is no track case today. The muse program (PR #593) took the line to five vendor flags and 75 of its 80-char cap; round 3 of that PR had to shorten the line's prose ("Ask a question, get a report" -> "Ask, get a report") just to fit. A sixth flag WILL breach the cap — the fix is a help-layout change (wrap the vendor list onto its own line or fold it into one `-<vendor>` token), and it touches the pinned help fixtures (`v2/conformance/cases/fixtures/cli-help/_root.txt`, `test/formatapps-help-toplevel-layout.test.js`), so it must be its own small PR done BEFORE the next vendor lands, never bundled into a vendor add. |
| 2026-09-02 | Make the onramp contract's two path-only rules (`surface-docs-required`, `runtime-smoke-required` in `src/shell/onramp.ts`) read the diff, not the file list, so a comment-only change does not demand a doc or smoke change | Parked, not rejected — serves no track on its own; it is gate care. The rot cleanup program (intent `project/docs/intent/2026-09-02-rot-cleanup.md`) hit both rules on comment-only PRs: PR 3 had to leave four surface files for PR 4, and PR 6a and 6b had to become one PR, because the contract counts a file by its path and never looks at what changed in it. The honest fix is a diff-aware rule (a change is "surface" or "runtime" only when a non-comment line changed), with a smoke that feeds it a comment-only diff and a code diff. Not an exemption flag — that would be a door around the gate. Same row: `citation-check` rule (c) reads `//` lines only in `test/`, by design (fixture strings use fake paths), so a dead repo path inside a test STRING is outside the gate; one such site was fixed by hand in PR 4. |
| 2026-09-02 | A stale `.cache/tsconfig.tsbuildinfo` can hide a real `dist/` drift | Parked, not rejected — build care. `tsc` incremental state in `.cache/` can make a rebuild skip files, so "rebuild, then `git status` is clean" proves nothing when the cache is old. The manager cleared `dist` and `.cache` before every dist check from PR 4 of the rot cleanup on. Fix: have the dist-drift check (`release:check` step) clear `.cache/` first, or build into a fresh temp dir and compare. |
