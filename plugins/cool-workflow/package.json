{
  "name": "cool-workflow",
  "version": "0.1.6",
  "description": "TypeScript COL-Architecture workflow orchestration for agent runtimes.",
  "type": "commonjs",
  "license": "BSD-2-Clause",
  "author": {
    "name": "COOLWHITE LLC"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node dist/cli.js list && node test/state-node-smoke.js && node test/pipeline-runner-smoke.js && node test/error-feedback-smoke.js && node test/worker-isolation-smoke.js && node test/candidate-scoring-smoke.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
