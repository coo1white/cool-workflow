* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.zip binary
*.tgz binary
*.gz binary
*.woff binary
*.woff2 binary

# Append-only logs: keep every side's entries on a parallel merge instead of
# conflicting. `union` is a BUILT-IN git merge driver (no local config needed),
# so it is honored both by local git and by GitHub's server-side merge. Two
# PRs that each append a section to one of these files then merge with no
# conflict, keeping both. Scope this to append-only logs ONLY: union does not
# dedupe or reorder, so it must never guard a file where line order or
# uniqueness carries meaning.
ITERATION_LOG.md merge=union
CHANGELOG.md merge=union

# GitHub Linguist: keep the Languages bar to JS/TS. These files are needed
# and cannot be JS (a Homebrew formula must be Ruby; the workbench UI serves
# its html/css from disk by design) -- this excludes them from the language
# STATS only, nothing about their behavior changes.
Formula/** linguist-vendored
plugins/cool-workflow/ui/workbench/*.css linguist-documentation
plugins/cool-workflow/ui/workbench/*.html linguist-documentation

