[
  { "kind": "subcommand", "id": "cw ledger propose", "behavior": "Build and print a digest-sealed proposal entry from --from/--to/--title/--rationale [--files a,b] [--diff patch]; diff bytes pass through with no trim", "evidence": "src/cli/handlers/ledger.ts:31", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "subcommand", "id": "cw ledger review", "behavior": "Build and print a digest-sealed review entry; --verdict approved|rejected (case-folded to upper), else throws '--verdict must be \"approved\" or \"rejected\".'", "evidence": "src/cli/handlers/ledger.ts:48", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "subcommand", "id": "cw ledger verify", "behavior": "Verify one entry from --file or stdin fd 0; prints LedgerVerifyResult JSON; exit 1 on ok:false; non-JSON input prints structured ledger-bad-json refusal", "evidence": "src/cli/handlers/ledger.ts:64", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "subcommand", "id": "cw ledger apply", "behavior": "Verify a proposal then print {ok,id,kind,diff,failedChecks}; diff non-null only when ok:true; exit 1 on ok:false", "evidence": "src/cli/handlers/ledger.ts:89", "tests": ["test/ledger-apply-smoke.js"] },
  { "kind": "subcommand", "id": "cw ledger list", "behavior": "Read+verify every *.json in --dir (sorted names); 2+ --dir union-verifies mirrors; exit 1 when allOk:false", "evidence": "src/cli/handlers/ledger.ts:113", "tests": ["test/ledger-verify-smoke.js", "test/ledger-resolution-smoke.js"] },
  { "kind": "subcommand", "id": "cw telemetry verify", "behavior": "Re-prove a run's telemetry.json chain (prevHash linkage + independent recordHash recompute); optional --pubkey re-runs ed25519 over each attested record; exit 1 when verified:false", "evidence": "src/cli/handlers/maintenance.ts:56", "tests": ["test/telemetry-ledger-smoke.js", "test/telemetry-verify-signatures-smoke.js", "test/tamper-evidence-demo-smoke.js"] },
  { "kind": "subcommand", "id": "cw audit verify", "behavior": "Re-prove the trust-audit event hash chain; prints {schemaVersion,runId,present,verified,eventCount,chained,unchained,corruptLines,failedChecks}; exit 1 when verified:false; absent chain exits 0", "evidence": "src/cli/handlers/audit.ts:19", "tests": ["test/audit-verify-smoke.js", "test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "subcommand", "id": "cw demo tamper", "behavior": "Hermetic ephemeral-ed25519 proof: build a real 3-hop signed ledger, forge it 3 ways (ledger verdict flip+reseal, inflated usage with old signature, edited signed result), show all caught; exit 1 when proven:false", "evidence": "src/cli/handlers/maintenance.ts:79", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "subcommand", "id": "cw demo bundle", "behavior": "Hermetic proof that a sealed bundle verifies offline with only its embedded public key and that chain/signature forgeries are caught; exit 1 when proven:false", "evidence": "src/cli/handlers/maintenance.ts:88", "tests": ["test/demo-bundle-smoke.js"] },
  { "kind": "subcommand", "id": "cw report verify-bundle", "behavior": "Offline self-contained verify of a .cwrun.json bundle: archive bytes + telemetry chain + trust-audit chain + embedded-key ed25519 + report-findings cross-check; always prints ReportBundleVerification JSON; exit 1 when ok:false", "evidence": "src/cli/handlers/operator.ts:24", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "subcommand", "id": "cw report bundle", "behavior": "Export a run sealed with the trust public key then immediately self-verify offline; prints ReportBundleResult JSON; exit 1 when ok:false", "evidence": "src/cli/handlers/operator.ts:33", "tests": ["test/report-bundle-smoke.js"] },
  { "kind": "flag", "id": "cw ledger propose --diff", "behavior": "Unified patch payload; presence detected trimmed but bytes passed verbatim (trailing newline kept for git apply); absent -> suggestedDiff \"\"", "evidence": "src/cli/handlers/ledger.ts:38", "tests": ["test/ledger-apply-smoke.js"] },
  { "kind": "flag", "id": "cw ledger propose --files", "behavior": "Comma-joined (or repeated) list coerced to trimmed non-empty string[] targetFiles", "evidence": "src/cli/handlers/ledger.ts:14", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "flag", "id": "cw ledger verify --file", "behavior": "Read the entry from a path; absent -> stdin (fd 0); read error throws 'Cannot read ledger entry...'", "evidence": "src/cli/handlers/ledger.ts:69", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "flag", "id": "cw ledger list --dir (repeatable)", "behavior": "One dir keeps single-dir output {dir,...}; 2+ dirs switch to union shape {dirs,...} with per-entry dirs mirror list", "evidence": "src/cli/handlers/ledger.ts:116", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "flag", "id": "cw telemetry verify --pubkey", "behavior": "Inline PEM or path; unresolvable input adds failed check {name:'signature-key',code:'telemetry-pubkey-unreadable'} and verified:false; falls back to CW_AGENT_ATTEST_PUBKEY", "evidence": "src/capability-core.ts:1191", "tests": ["test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "flag", "id": "cw report verify-bundle --pubkey", "behavior": "Key override used only when the bundle embeds no trust key (precedence bundle > argument > environment)", "evidence": "src/run-export.ts:484", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "flag", "id": "cw report verify-bundle --extract-report", "behavior": "Write the bundle's report.md to a path; requested-but-unfulfilled fails with extract-report/report-md-unavailable; escape of cwd fails path-outside-working-directory", "evidence": "src/run-export.ts:560", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "flag", "id": "cw report verify-bundle --strict-signatures", "behavior": "Fail (signatures/signature-key-required) when the bundle claims attested telemetry but no key is available to re-verify", "evidence": "src/run-export.ts:581", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "flag", "id": "cw report verify-bundle --require-signatures", "behavior": "Fail (signatures/signatures-required) when trustLevel is 'unsigned' (closes the intact-but-unsigned fail-open)", "evidence": "src/run-export.ts:599", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "flag", "id": "cw report bundle --with-trust-key", "behavior": "ed25519 PUBLIC key (path on the CLI; inline PEM programmatic/env) embedded in the archive trust block; defaults to CW_AGENT_ATTEST_PUBKEY", "evidence": "src/capability-core.ts:291", "tests": ["test/report-bundle-smoke.js"] },
  { "kind": "flag", "id": "--json (telemetry verify, demo tamper, demo bundle)", "behavior": "Print the structured result as JSON instead of the human render", "evidence": "src/cli/handlers/maintenance.ts:62", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "env", "id": "CW_AGENT_ATTEST_PUBKEY", "behavior": "Operator trust PUBLIC key (inline PEM or .pem path): record-time verify default, telemetry-verify default key, export/bundle seal default, verify-bundle last-place key", "evidence": "src/telemetry-attestation.ts:124", "tests": ["test/telemetry-attestation-smoke.js", "test/report-bundle-smoke.js"] },
  { "kind": "env", "id": "CW_REQUIRE_RESOLVABLE_EVIDENCE", "behavior": "Default ON (unset/empty=true): file-style evidence locators must resolve on disk; 0/false/no/off = shape-only; value containing 'url' also turns on URL reachability", "evidence": "src/evidence-grounding.ts:56", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "env", "id": "CW_REQUIRE_ARCHIVE_INTEGRITY", "behavior": "1/true/yes/on: refuse an archive whose top-level integrity block is absent, at import (throw) and at inspect (failed check archive-integrity-required)", "evidence": "src/run-export.ts:831", "tests": ["test/run-import-tamper-failclosed-smoke.js"] },
  { "kind": "exit_code", "id": "ledger verify/apply exit 1 on ok:false", "behavior": "Tampered/malformed/non-JSON entry (or non-proposal / empty diff for apply) sets process.exitCode=1", "evidence": "src/cli/handlers/ledger.ts:86", "tests": ["test/ledger-verify-smoke.js", "test/ledger-apply-smoke.js"] },
  { "kind": "exit_code", "id": "ledger list exit 1 on allOk:false", "behavior": "Any failing entry in any dir/mirror fails the whole inbox with exit 1", "evidence": "src/cli/handlers/ledger.ts:127", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "exit_code", "id": "telemetry verify exit 1 on verified:false", "behavior": "Forged/edited/corrupt ledger or failed signature re-check exits 1; absent ledger (present:false/verified:true) exits 0", "evidence": "src/cli/handlers/maintenance.ts:67", "tests": ["test/tamper-evidence-demo-smoke.js", "test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "exit_code", "id": "audit verify exit 1 on verified:false", "behavior": "Broken/edited/mixed-era/corrupt-line audit chain exits 1; absent chain exits 0; audit summary always exits 0", "evidence": "src/cli/handlers/audit.ts:28", "tests": ["test/audit-verify-smoke.js"] },
  { "kind": "exit_code", "id": "demo tamper/bundle exit 1 on proven:false", "behavior": "If any forgery goes undetected the demo exits 1 (a regression in the integrity guarantee)", "evidence": "src/cli/handlers/maintenance.ts:85", "tests": ["test/tamper-evidence-demo-smoke.js", "test/demo-bundle-smoke.js"] },
  { "kind": "exit_code", "id": "report verify-bundle / report bundle exit 1 on ok:false", "behavior": "A forged, edited, unreadable, or unverifiable bundle exits 1 so '&& ship' cannot pass on a lie", "evidence": "src/cli/handlers/operator.ts:30", "tests": ["test/report-verify-bundle-smoke.js", "test/report-bundle-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_ledger_propose", "behavior": "buildLedgerProposal from from/to/title/rationale/files(comma)/diff; createdAt=now; returns the sealed entry object", "evidence": "src/mcp/tool-call.ts:351", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_ledger_review", "behavior": "buildLedgerReview; same verdict check/message as the CLI", "evidence": "src/mcp/tool-call.ts:361", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_ledger_verify", "behavior": "verifyLedgerEntry(args.entry) over the entry OBJECT; returns LedgerVerifyResult", "evidence": "src/mcp/tool-call.ts:373", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_ledger_apply", "behavior": "applyLedgerProposal(args.entry); diff only when the entry verifies as a proposal with a diff", "evidence": "src/mcp/tool-call.ts:375", "tests": ["test/ledger-apply-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_ledger_list", "behavior": "listLedgerEntries(dir) or unionLedgerEntries(dirs) when 2+ dirs given", "evidence": "src/mcp/tool-call.ts:377", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_telemetry_verify", "behavior": "Peer of cw telemetry verify: runId + optional pubkey; same TelemetryVerifyResult", "evidence": "src/mcp/tool-definitions.ts:1002", "tests": ["test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_audit_verify", "behavior": "Peer of cw audit verify over the trust-audit chain", "evidence": "src/mcp/tool-definitions.ts:399", "tests": ["test/audit-verify-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_report_verify_bundle", "behavior": "Peer of cw report verify-bundle; restores into a throwaway tmpdir, writes nothing to any registry", "evidence": "src/mcp/tool-definitions.ts:871", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "mcp_tool", "id": "cw_report_bundle", "behavior": "Peer of cw report bundle (produce-and-prove); defaults the seal key to CW_AGENT_ATTEST_PUBKEY", "evidence": "src/mcp/tool-definitions.ts:881", "tests": ["test/report-bundle-smoke.js"] },
  { "kind": "stdout_format", "id": "sealed ledger entry shape", "behavior": "Proposal: kind,schemaVersion:1,from,to,title,rationale,targetFiles,suggestedDiff,createdAt,id,digest; review swaps in target,verdict(APPROVED|REJECTED),findings", "evidence": "src/ledger.ts:121", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "stdout_format", "id": "LedgerVerifyResult", "behavior": "{ok,id,kind,checks[{name,pass,code?,detail?}],failedChecks}; check order structure,kind,schema,digest-present,fields,digest,id; stops at first failure", "evidence": "src/ledger.ts:159", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "stdout_format", "id": "LedgerApplyResult", "behavior": "{ok,id,kind,diff,failedChecks}; diff:null unless ok:true", "evidence": "src/ledger.ts:217", "tests": ["test/ledger-apply-smoke.js"] },
  { "kind": "stdout_format", "id": "LedgerListResult + resolution", "behavior": "{dir,count,allOk,entries[{file,id,kind,from,to,title,target,verdict,ok,failedChecks}],resolution{proposals[{id,title,resolution,reviews}],pending,approved,rejected,contested}}", "evidence": "src/ledger.ts:299", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "stdout_format", "id": "LedgerUnionResult", "behavior": "{dirs,count,allOk,entries(+per-entry dirs),resolution}; verified entries de-duplicated by content-addressed id; failures kept per occurrence", "evidence": "src/ledger.ts:354", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "stdout_format", "id": "TelemetryVerifyResult JSON", "behavior": "{schemaVersion:1,runId,present,verified,records,attested,unattested,absent,signatureKeyProvided,signaturesChecked,signaturesReverified,signaturesFailed,failedChecks[{name,code}]}", "evidence": "src/telemetry-demo.ts:55", "tests": ["test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "stdout_format", "id": "formatTelemetryVerify human text", "behavior": "Starts 'telemetry verify <runId>'; heads '✓ VERIFIED — N record(s), chain intact, every hash recomputed independently' / '✗ TAMPERING DETECTED — N check(s) failed' / '✗ VERIFICATION REFUSED — supplied public key was unreadable'; tally '   attested a · unattested u · absent b'", "evidence": "src/telemetry-demo.ts:79", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "stdout_format", "id": "TamperDemoResult JSON", "behavior": "{schemaVersion:1,runId:'demo-tamper-run',workers:3,trustKey:'ephemeral-ed25519',baseline{ledgerVerified,signaturesValid,records},layers[{layer,tamper,before,after,failures}],proven}", "evidence": "src/telemetry-demo.ts:43", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "stdout_format", "id": "formatTamperDemo human text", "behavior": "First line 'cw demo tamper — tamper-evidence proof (hermetic, ephemeral-ed25519 key)'; per-layer '▶ LEDGER|SIGNATURE|RESULT tamper' with edit/before/after; verdict 'VERDICT: tamper-evidence holds ✓ — ...' or 'VERDICT: PROOF FAILED ✗ — ...'", "evidence": "src/telemetry-demo.ts:100", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "stdout_format", "id": "BundleDemoResult JSON + formatBundleDemo", "behavior": "{...runId:'demo-bundle-run',baseline{ok,telemetryVerified,signaturesReverified},layers(chain|signature),proven}; verdict 'VERDICT: bundle verification holds ✓ — ...' / 'VERDICT: PROOF FAILED ✗ — a forged bundle verified. ...'", "evidence": "src/telemetry-demo.ts:283", "tests": ["test/demo-bundle-smoke.js"] },
  { "kind": "stdout_format", "id": "ReportBundleVerification JSON", "behavior": "{schemaVersion,archivePath,runId,ok,archiveOk,telemetryVerified,trustAuditVerified,trustKeySource(bundle|argument|environment|none),signatureKeyProvided,signaturesChecked,signaturesReverified,signaturesFailed,trustLevel(signed|unsigned),reportFindingsVerified,reportExtractedTo?,failedChecks}", "evidence": "src/types/report-bundle.ts:9", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "stdout_format", "id": "audit verify JSON", "behavior": "{schemaVersion:1,runId,present,verified,eventCount,chained,unchained,corruptLines,failedChecks}", "evidence": "src/capability-core.ts:1230", "tests": ["test/audit-verify-smoke.js"] },
  { "kind": "stdout_format", "id": "formatEvidenceReasoningReport", "behavior": "Lines 'Evidence Adoption Reasoning: <runId>', 'Freshness: <status>', 'Adoption Rationale', tally 'chains=..; explained=..; unexplained=..; n/a=..; adopted=..; rejected=..', max 60 chains then '... N more', 'Next Action'", "evidence": "src/evidence-reasoning.ts:646", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "state_file", "id": ".cw/runs/<id>/telemetry.json", "behavior": "Append-only hash-chained TelemetryLedger {schemaVersion:1,runId,records[]}; durable temp->fsync->rename writes; never rewritten in place", "evidence": "src/telemetry-ledger.ts:25", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "state_file", "id": ".cw/runs/<id>/audit/events.jsonl", "behavior": "One TrustAuditEvent JSON per line; durable (fsync) append; hash-chained via prevEventHash/eventHash", "evidence": "src/trust-audit.ts:283", "tests": ["test/audit-verify-smoke.js", "test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "state_file", "id": ".cw/runs/<id>/audit/summary.json and audit/index.json", "behavior": "Durable derived views written by summarizeTrustAudit; index rows carry all correlation ids except scoreId", "evidence": "src/trust-audit.ts:488", "tests": ["test/multi-agent-trust-policy-audit-smoke.js"] },
  { "kind": "state_file", "id": ".cw/runs/<id>/reasoning/{index.json,report.json,chain-<id>.json}", "behavior": "Derived reasoning index (id:'evidence-reasoning-index') + per-chain files + report with freshness forced 'valid' on refresh", "evidence": "src/evidence-reasoning.ts:533", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "state_file", "id": "ledger directory <dir>/*.json", "behavior": "One self-verifying entry per .json file; kernel READS only; writing + git is the operator's step", "evidence": "src/ledger.ts:249", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "state_file", "id": "*.cwrun.json bundle trust block", "behavior": "Optional trust:{publicKeyPem,algorithm:'ed25519'} embedded at export; only ever a PUBLIC key", "evidence": "src/run-export.ts:127", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "ledger digest + content-addressed id", "behavior": "digest = 'sha256:'+hex over recursively-key-sorted JSON of all fields except id/digest; id = 'ldg-'+first 16 hex of digest", "evidence": "src/ledger.ts:102", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "behavior", "id": "ledger verify failure codes", "behavior": "ledger-not-object, ledger-unknown-kind, ledger-bad-schema, ledger-missing-digest, ledger-missing-field, ledger-bad-verdict, ledger-digest-mismatch, ledger-id-mismatch (+ CLI ledger-bad-json)", "evidence": "src/ledger.ts:172", "tests": ["test/ledger-verify-smoke.js"] },
  { "kind": "behavior", "id": "apply gate codes", "behavior": "ledger-not-a-proposal (a review) and ledger-empty-diff (no suggestedDiff) both yield ok:false diff:null", "evidence": "src/ledger.ts:238", "tests": ["test/ledger-apply-smoke.js"] },
  { "kind": "behavior", "id": "list per-entry failure codes", "behavior": "ledger-dir-unreadable (failure entry file=dir path, count 0), ledger-entry-not-regular (non-plain file), ledger-bad-json (unreadable/unparseable)", "evidence": "src/ledger.ts:311", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "behavior", "id": "inbox resolution states", "behavior": "pending (no verified review targets it) / approved / rejected (all verified reviews agree) / contested (they disagree); only VERIFIED entries take part — a tampered review keeps the proposal pending", "evidence": "src/ledger.ts:400", "tests": ["test/ledger-resolution-smoke.js"] },
  { "kind": "behavior", "id": "sha256 helper form", "behavior": "sha256(value) returns 'sha256:<hex>' (prefixed); archive file digests use bare hex — two distinct domains", "evidence": "src/execution-backend/util.ts:13", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "telemetry genesis hash", "behavior": "record 1 prevHash = sha256('cw-telemetry-ledger:'+runId)", "evidence": "src/telemetry-ledger.ts:75", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "recordId is chain position", "behavior": "recordId = 'tel-'+seq zero-padded to 3 (tel-001, ...); never wall clock or global counter", "evidence": "src/telemetry-ledger.ts:112", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "recordHash canonical field set", "behavior": "sha256(stableStringify({schemaVersion,runId,recordId,recordedAt,workerId,taskId,promptDigest,reportedUsageDigest,[reportedUsage if present],usageSignature||null,[resultDigest if present],attestation,attestationReason||null,prevHash})); absent optional keys are OMITTED for byte-back-compat", "evidence": "src/telemetry-ledger.ts:81", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "reportedUsageDigest of absent usage", "behavior": "Absent usage digests as stableStringify(null) — 'the agent reported nothing' is itself bound", "evidence": "src/telemetry-ledger.ts:108", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "absent vs corrupt telemetry ledger", "behavior": "Absent file = empty chain (present:false, verified:true); present-but-unparseable = corrupt: verify reports telemetry-ledger-corrupt (present:true, verified:false) and append/load THROWS TelemetryLedgerCorruptError — never re-genesis", "evidence": "src/telemetry-ledger.ts:50", "tests": ["test/telemetry-ledger-smoke.js"] },
  { "kind": "behavior", "id": "verifyTelemetryLedger checks", "behavior": "chain-link[i]/telemetry-chain-broken on prevHash miss; record-hash[i]/telemetry-digest-mismatch on independent recompute miss; never trusts the stored hash", "evidence": "src/telemetry-ledger.ts:206", "tests": ["test/telemetry-ledger-smoke.js", "test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "canonical signed payload", "behavior": "stableStringify({usage:usage??null,runId,taskId,promptDigest,[resultDigest only when given]}) — keys emerge sorted; ed25519 sign/verify with null digest arg; signature base64", "evidence": "src/telemetry-attestation.ts:63", "tests": ["test/telemetry-attestation-smoke.js"] },
  { "kind": "behavior", "id": "attestation statuses + exact reasons", "behavior": "absent:'agent reported no usage'; unattested reasons: 'reported usage carries no signature', 'no trust key configured (set agent attestPublicKey / CW_AGENT_ATTEST_PUBKEY)', 'trust key unreadable: <msg>', 'signature is empty', 'signature is not valid base64', 'verification error: <msg>', 'signature does not match reported usage (tampered, replayed, or wrong key)'", "evidence": "src/telemetry-attestation.ts:111", "tests": ["test/telemetry-attestation-smoke.js"] },
  { "kind": "behavior", "id": "4-field fallback + coversResult", "behavior": "Try 5-field payload (with resultDigest) first; on miss retry 4-field for old signers; coversResult:true only on a first-arm match; a new result-covering signer fails BOTH arms when the result was edited", "evidence": "src/telemetry-attestation.ts:139", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "CW holds no private key", "behavior": "signTelemetry is an executor-side helper the CW runtime never calls; CW/auditors verify with the operator-provisioned PUBLIC key only", "evidence": "src/telemetry-attestation.ts:179", "tests": ["test/telemetry-attestation-smoke.js", "test/readme-trust-claim-smoke.js"] },
  { "kind": "behavior", "id": "resolveTrustPublicKey", "behavior": "Inline PEM (contains BEGIN and KEY) or a readable file path; anything else resolves undefined (=> unattested), never a throw", "evidence": "src/telemetry-attestation.ts:164", "tests": ["test/telemetry-attestation-smoke.js"] },
  { "kind": "behavior", "id": "verifyTelemetrySignatures codes", "behavior": "signature[i] checks: pass/informational signature-unchecked-no-key without a key; fail telemetry-usage-unavailable (attested but no raw usage), telemetry-usage-digest-mismatch, telemetry-pubkey-unreadable, telemetry-signature-mismatch", "evidence": "src/telemetry-attestation.ts:266", "tests": ["test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "behavior", "id": "resultBound anchoring rule", "behavior": "resultBound records {taskId,resultDigest} only when the signature re-verified AND covered the result (5-field); a 4-field match never anchors an injected resultDigest", "evidence": "src/telemetry-attestation.ts:297", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "telemetry verify verdict conjunction", "behavior": "verified = chain ok AND key input (if any) readable AND signaturesFailed===0; no key => chain-only, unchanged legacy behavior", "evidence": "src/capability-core.ts:1204", "tests": ["test/telemetry-verify-signatures-smoke.js"] },
  { "kind": "behavior", "id": "demo tamper layer A (ledger)", "behavior": "Flip records[1] 'unattested'->'attested' AND reseal its recordHash: per-record digest passes but chain-link[2] breaks (telemetry-chain-broken) — one resealed record cannot hide without rewriting all after it", "evidence": "src/telemetry-demo.ts:186", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "demo tamper layer B (signature)", "behavior": "Inflate output_tokens 10x, keep the original ed25519 signature: verify rejects with 'signature does not match reported usage (tampered, replayed, or wrong key)'", "evidence": "src/telemetry-demo.ts:207", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "demo tamper layer C (result)", "behavior": "Sign with resultDigest=sha256(finding); edit 'severity HIGH'->'severity LOW'; CW re-derives the digest so the signature joins neither the 5-field nor the 4-field payload", "evidence": "src/telemetry-demo.ts:229", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "demo proven rule", "behavior": "proven = clean baseline verified AND all signed hops verified AND every layer flips before.verified:true -> after.verified:false with >=1 failure", "evidence": "src/telemetry-demo.ts:256", "tests": ["test/tamper-evidence-demo-smoke.js", "test/demo-bundle-smoke.js"] },
  { "kind": "behavior", "id": "demo fixed inputs", "behavior": "3 hops (w-map/attested, w-assess/unattested, w-verdict/attested), fixed token counts, DEMO_NOW=2026-01-01T00:00:00.000Z, runIds demo-tamper-run/demo-bundle-run, ephemeral keypair per run, private tmpdir removed unless keepDir", "evidence": "src/telemetry-demo.ts:133", "tests": ["test/tamper-evidence-demo-smoke.js"] },
  { "kind": "behavior", "id": "verify-bundle key precedence", "behavior": "bundle-embedded key > --pubkey argument > CW_AGENT_ATTEST_PUBKEY env; trustKeySource reports which won", "evidence": "src/run-export.ts:484", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "verify-bundle ok conjunction", "behavior": "ok = archive bytes ok AND telemetry chain AND trust-audit chain AND signaturesFailed===0 AND reportFindings ok AND no strict/extract/require shortfall", "evidence": "src/run-export.ts:614", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "report-findings cross-check", "behavior": "Driven by sig.resultBound (signature-anchored), never run.tasks; per bound task fails name 'report-findings' with code result-missing:<task> / result-digest-mismatch:<task> / report-result-mismatch:<task>; report anchor is the task's own '### <taskId>' + 'Result:' section, body-first, every occurrence walked", "evidence": "src/run-export.ts:527", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "trustLevel signed/unsigned", "behavior": "'signed' only when >=1 result-covering signature re-verified, none failed, and the cross-check held; usage-only, unverifiable, or tampered => 'unsigned'; scope is the SIGNED findings, not report exhaustiveness", "evidence": "src/run-export.ts:597", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "verify-bundle never throws", "behavior": "Missing/unreadable/non-JSON/wrong-schema bundle => ok:false with structured checks and no restore; restore errors become a failed 'restore' check; tmpdir cw-verify-bundle-* always removed", "evidence": "src/run-export.ts:462", "tests": ["test/report-verify-bundle-smoke.js"] },
  { "kind": "behavior", "id": "trust-audit genesis + eventHash", "behavior": "genesis = sha256('cw-trust-audit:'+runId); eventHash = sha256(stableStringify(JSON.parse(JSON.stringify(event sans eventHash)))) — the JSON round-trip drops undefined so record-time == verify-time", "evidence": "src/trust-audit.ts:68", "tests": ["test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "behavior", "id": "trust-audit verify codes", "behavior": "trust-audit-corrupt-line (bad line, counted, verified:false), event-hash[i]/trust-audit-digest-mismatch, chain-link[i]/trust-audit-chain-broken, unchained-events/trust-audit-unchained-event", "evidence": "src/trust-audit.ts:117", "tests": ["test/audit-verify-smoke.js", "test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "behavior", "id": "trust-audit era rule", "behavior": "All-legacy logs verify with unchained counts (not failed); ONE hash-less event mixed into a chained log fails as a forgery attempt", "evidence": "src/trust-audit.ts:145", "tests": ["test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "behavior", "id": "trust-audit event id determinism", "behavior": "id = 'audit-'+safeFileName(kind)+'-'+count padded to 4 (chain position, no clock); id is bound into the eventHash chain", "evidence": "src/trust-audit.ts:673", "tests": ["test/freebsd-audit-fixes-smoke.js"] },
  { "kind": "behavior", "id": "durable audit append", "behavior": "recordTrustAuditEvent chains (prevEventHash to prior or genesis), hashes, then durableAppendFileSync (fsync) one JSON line; summary/index rewritten durably after", "evidence": "src/trust-audit.ts:326", "tests": ["test/audit-verify-smoke.js"] },
  { "kind": "behavior", "id": "metadata scrub", "behavior": "Keys matching /secret|token|password|credential|authorization|api[_-]?key/i become '[redacted]'; array strings with '=' keep only the pre-'=' part; empty result omitted", "evidence": "src/trust-audit.ts:694", "tests": ["test/multi-agent-trust-policy-audit-smoke.js"] },
  { "kind": "behavior", "id": "grounded evidence shapes", "behavior": "URL, path-like (has / or \\), file-ext locator with optional :line(-line), or namespace:value token; bare prose is ungrounded", "evidence": "src/evidence-grounding.ts:27", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "evidence confidence tiers", "behavior": "ungrounded < grounded < resolvable < verified; 'verified' is NEVER auto-assigned (needs explicit host attestation); file locators upgrade to resolvable only when they stat on disk", "evidence": "src/evidence-grounding.ts:113", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "extractEvidenceContent", "behavior": "For 'file.ts:42' return line 42 (1-indexed); no line => first 200 chars; undefined when non-file or unreadable — never fabricates", "evidence": "src/evidence-grounding.ts:153", "tests": ["test/evidence-content-extraction-smoke.js"] },
  { "kind": "behavior", "id": "taskRequiresEvidence", "behavior": "requiresEvidence flag OR task id matching /^verify[:\\/]/i, /^verdict[:\\/]/i, /^synthesis[:\\/]/i", "evidence": "src/verifier.ts:30", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "validateResultEnvelope throws", "behavior": "Required-evidence task without grounded evidence throws 'Task <id> requires grounded cw:result evidence (a path-like locator, URL, or namespace:value token — not free text)'; P0/P1/P2 findings need grounded evidence; findings need id + valid classification; declared schema violations throw (drive parks the hop)", "evidence": "src/verifier.ts:39", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "phase and verdict gates", "behavior": "assertTaskCanComplete blocks a task outside the first runnable phase or from a non pending/running status; validateRunGates blocks a completed verdict/synthesis task whose earlier phase is incomplete", "evidence": "src/verifier.ts:7", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "emptyCaptureWarning no-false-green gate", "behavior": "Reads the verifier node's source result node (inputs.inputNodeId else first parent) metadata.captureWarning; shared by selection AND commit so a zero-evidence result can never present as green", "evidence": "src/gates.ts:29", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "sandboxProfileForCandidate", "behavior": "Worker's sandboxProfileId when the candidate has a worker, else the originating task's; pure over persisted state", "evidence": "src/gates.ts:43", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "reasoning fails closed to unexplained", "behavior": "A decision step with no recorded reason renders {status:'unexplained'}; an adopted/rejected item with NO decision step gets an explicit unexplained step; roll-up: ANY unexplained decision step => chain 'unexplained'; reason text '<gate>: no recorded rationale for <decision> adoption'", "evidence": "src/evidence-reasoning.ts:182", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "behavior", "id": "reasoning freshness + nextAction", "behavior": "absent (no persisted index) / valid / stale (fingerprint drift); nextAction 'node scripts/cw.js multi-agent reasoning <run> --refresh' when stale/absent, '--json' when unexplained>0, else 'multi-agent evidence <run> --json'", "evidence": "src/evidence-reasoning.ts:91", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "behavior", "id": "reasoning fingerprint", "behavior": "'sha256:'+first 32 hex of sha256 over the sorted JSON lines of (id,evidenceStatus,rationaleStatus,steps[gate,decision,rationale.status,sourceId])", "evidence": "src/evidence-reasoning.ts:762", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "behavior", "id": "malformed score records skipped", "behavior": "readAllScores drops unreadable or shape-invalid score JSON; the score gate then fails closed rather than trusting a forged score", "evidence": "src/evidence-reasoning.ts:743", "tests": ["test/evidence-adoption-reasoning-smoke.js"] },
  { "kind": "behavior", "id": "record-time attestation flow", "behavior": "attestWorkerDelegation verifies the agent's signature with resultDigest=sha256(accepted result bytes); recordWorkerDelegationLedger appends the chained record BEFORE the audit event and stores resultDigest only when coversResult; audit event cross-links telemetryRecordId/RecordHash/PrevHash", "evidence": "src/worker-accept/telemetry-ledger.ts:34", "tests": ["test/telemetry-attestation-smoke.js"] },
  { "kind": "behavior", "id": "requireAttestedTelemetry opt-in gate", "behavior": "When on, a delegated hop whose verdict is not 'attested' is rejected pre-mutation with code telemetry-unattested-blocked and the drive parks it; default off (flag-and-surface)", "evidence": "src/worker-accept/telemetry-ledger.ts:51", "tests": ["test/telemetry-fail-closed-smoke.js"] },
  { "kind": "behavior", "id": "commit gate evidence codes", "behavior": "commit-verifier-missing-evidence, commit-verifier-evidence-ungrounded, commit-verifier-evidence-unresolvable (strict mode); worker accept adds worker-evidence-unresolvable", "evidence": "src/commit.ts:359", "tests": ["test/verifier-gated-commit-smoke.js"] },
  { "kind": "behavior", "id": "local-chain honest limit", "behavior": "A determined local writer re-chaining the WHOLE log with the module's own sha256 is not caught; the only crypto anchor is the agent's signature; CW cannot mint an anchor for itself (holds no private key)", "evidence": "src/trust-audit.ts:30", "tests": ["test/readme-trust-claim-smoke.js"] }
]
